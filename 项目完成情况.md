# Literature Agent - 项目完成情况

## 2026-06-22 - Module 0 核心契约与公共基础

### 已实现

- 创建 Rust workspace 和 `src-tauri` 后端库骨架。
- 新增 `src-tauri/src/core` 核心契约模块。
- 实现 typed ports、`PortValue` 引用传递、`TextRange`、必填端口校验。
- 实现 Workflow、Node、Edge、RunControl、RunStatus、NodeRunState。
- 实现受限循环 `LoopPolicy`，包含最大轮次、超时、预算和停止条件校验。
- 实现 Artifact 描述、来源引用和 Document Patch 数据结构。
- 实现 Node、Skill、Provider 三类注册表及重复/缺失错误。
- 实现统一 `CoreError` 错误模型。
- 实现运行事件 `EventEnvelope` 和核心事件枚举。
- 实现资源限制、资源池、资源许可和取消信号。
- 实现 Auto Mode、节点审批策略、权限硬限制和执行策略。
- 补充 Module 0 契约说明文档: `docs/module-0-contracts.md`。
- 增加核心模块单元测试和 `src-tauri/tests/core_contracts.rs` 集成测试。

### 验证情况

- 已安装项目内 Rust 工具链: `.cargo` / `.rustup`。
- 已执行 `cargo fmt`。
- 已执行 `cargo test`: 10 个单元测试、3 个集成测试全部通过。

### 后续

- Module 1 可在 Module 0 公共类型基础上继续实现配置、密钥引用和迁移。

## 2026-06-22 - Module 1 配置管理、密钥存储与迁移

### 已实现

- 新增 `src-tauri/src/config` 配置模块。
- 实现 `.config` 分文件 YAML 配置布局:
  `app.yaml`、`providers.yaml`、`permissions.yaml`、`rag.yaml`、`workflow.yaml`、`git.yaml`、`auto_mode.yaml`。
- 实现 `ProjectConfig` 聚合配置模型和默认配置。
- 实现 provider 配置，支持 OpenAI、Anthropic、Gemini、OpenAI-compatible、Local 等 provider 类型。
- OpenAI-compatible provider 支持并校验 `base_url`。
- 实现 provider model 能力配置，复用 Module 0 的 `ProviderCapability`。
- 实现权限配置与 Auto Mode 配置分离，Auto Mode 不改写权限开关。
- 实现 `SecretRef { key_id }`，项目配置只保存 key id，不保存真实 secret。
- 实现 `SecretStore` 抽象、`MemorySecretStore` 测试后端。
- 增加可选 `system-keychain` feature，通过 `keyring` crate 接入系统 keychain。
- 实现 schema version 读取和幂等迁移，当前版本为 1。
- 实现 `ConfigStore::load_or_create`、`load`、`save`、`migrate_all`。
- 补充 Module 1 文档: `docs/module-1-config.md`。
- 增加配置模块单元测试和 `src-tauri/tests/config_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test`: 15 个单元测试、6 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

### 后续

- Module 2 可继续在配置和核心错误模型基础上实现成本账本、预算限制和 Auto Mode 预授权检查。

## 2026-06-22 - Module 2 成本追踪与预算控制

### 已实现

- 新增 `src-tauri/src/costs` 成本模块。
- 实现成本分类: LLM、Embedding、Reranker、Search API、HTTP Skill、Approval、Other。
- 实现 `NewCostRecord`、`CostRecord`、`CostQuery`、`TokenUsage`。
- 实现 token 成本估算，支持从 Module 1 的 `ModelConfig` 读取每百万 token 价格。
- 实现预算决策:
  单次预算、每日预算、月度预算、普通模式高成本确认、Auto Mode 预授权预算。
- 超预算决策返回 `RunControl::Pause`。
- 普通模式高成本操作返回 `RequireConfirmation` 并暂停等待确认。
- 实现 SQLite 成本账本 `SqliteCostLedger`，默认数据库文件为 `costs.db`。
- 实现成本数据库幂等迁移和 `schema_migrations` 记录。
- 成本记录支持 provider、model、workflow、run、node、tool_call_id、tokens 和 metadata。
- 补充 Module 2 文档: `docs/module-2-costs.md`。
- 增加成本模块单元测试和 `src-tauri/tests/cost_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test`: 20 个单元测试、8 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

### 后续

- Module 3 可继续实现 Provider trait，并要求 provider 调用统一写入 Module 2 成本账本。

## 2026-06-22 - 本地 Git 仓库初始化

### 已实现

- 扩充 `.gitignore`，忽略构建产物、本地 Rust 工具链、缓存、运行库、索引库、本地配置、数据库和环境变量文件。
- 将 Rust package/lib 名称改为 `ariadne`，对应线上仓库 `Ariadne`。
- 更新集成测试引用到 `ariadne` crate。
- 用户已初始化标准 `.git` 仓库；已推送到 `git@github.com:yanshaoqwq/Ariadne.git` 的 `main` 分支。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 20 个单元测试、8 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

## 2026-06-23 - Module 3 Provider 抽象与模型接入

### 已实现

- 新增 `src-tauri/src/providers` Provider 抽象模块。
- 实现标准 LLM 请求/响应结构: messages、content parts、tool definitions、tool calls、usage、cost、raw payload。
- 实现 Embedding、Reranker、SearchProvider 请求/响应结构。
- 定义 `ProviderCallContext`，包含 provider、workflow、run、node、tool_call、timeout、retry 和 metadata。
- 定义四类 Provider trait:
  `LlmProvider`、`EmbeddingProvider`、`RerankerProvider`、`SearchProvider`。
- 实现 `ProviderRuntimeRegistry`，支持 LLM/Embedding/Reranker/Search provider 按 id 独立切换。
- 实现 `ProviderProtocol` 和 `ToolUseEnvelope`，封装 OpenAI-compatible、Anthropic、Gemini 的 tool-use 差异。
- OpenAI-compatible provider 复用 OpenAI 协议族，且通过 `base_url` 支持本地模型服务。
- 实现 `ProviderExecutor`，Provider 响应包含 `cost_usd` 时自动写入 Module 2 `CostLedger`。
- 补充 Module 3 文档: `docs/module-3-providers.md`。
- 增加 `src-tauri/tests/provider_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 22 个单元测试、12 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

### 后续

- Module 4 可继续实现 VectorStore、FullTextStore 和 Reranker 后端抽象。

## 2026-06-23 - 项目检验报告回应与改进

### 已实现

- 阅读并回应 `项目检验报告/` 下关于逻辑错误、计划缺失和架构一致性的合理问题。
- 强化 `LoopPolicy`:
  - 增加每轮最小时长约束，避免“高轮次 + 极短超时”的无效循环配置。
  - 增加与 Module 1 workflow 配置上限的交叉校验。
- 强化路径权限检查:
  - 对绝对路径做词法规范化。
  - 对已存在路径前缀做 canonicalize。
  - 增加父目录逃逸和符号链接逃逸测试。
- 成本估算新增区间结果:
  - 已知 tool-use 轮次时给出较窄区间。
  - 未知 tool-use 轮次时给出保守区间和较低置信度，避免展示为精确费用。
- Provider 抽象新增生命周期和诊断能力:
  - `initialize`
  - `health_check`
  - `shutdown`
  - registry 统一收集 lifecycle / health 报告。
- 更新模块文档:
  - `docs/module-0-contracts.md`
  - `docs/module-2-costs.md`
  - `docs/module-3-providers.md`
- 新增审查回应文档:
  - `docs/report-response-2026-06-23.md`
- 将计划缺失项补入主计划:
  - `模块化实施计划.md`
  - `项目总计划-架构版(禁止删除).md`

### 计划补充范围

- 数据迁移与升级策略。
- 错误恢复与灾难恢复。
- 安全、隐私与审计。
- 性能监控与诊断。
- 测试策略细节。
- 分发与依赖管理。
- Qdrant sidecar 生命周期管理。
- runtime 恢复、索引重建、前端诊断和用户引导。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 27 个单元测试、13 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

### 后续

- 按更新后的计划继续 Module 4，实现 VectorStore、FullTextStore、Reranker，并优先细化 Qdrant sidecar 生命周期和索引恢复路径。

## 2026-06-23 - Module 4 向量、全文与重排检索

### 已实现

- 新增 `src-tauri/src/retrieval` 检索模块，并在 `src-tauri/src/lib.rs` 导出。
- 定义检索数据模型:
  - `ChunkDocument`
  - `VectorRecord`
  - `FullTextRecord`
  - `RetrievalResult`
  - `VectorSearchRequest`
  - `FullTextSearchRequest`
  - `HybridSearchRequest`
  - `StoreHealth`
  - `RebuildReport`
- 定义稳定接口:
  - `VectorStore`
  - `FullTextStore`
  - `ResultReranker`
  - `HybridSearch`
- 实现测试和早期集成可用的内存后端:
  - `MemoryVectorStore`
  - `MemoryFullTextStore`
- 实现向量相似度检索、全文关键词检索、metadata filter、按 document 删除、索引重建标记和重建报告。
- 实现 `HybridSearchEngine`:
  - 合并向量和全文结果。
  - 按 `chunk_id` 去重。
  - 保留来源引用、score、snippet 和 metadata。
  - 支持可插拔 reranker。
- 实现 reranker 层:
  - `ScoreReranker`
  - `ProviderResultReranker`，适配 Module 3 `RerankerProvider`。
- 实现 Qdrant sidecar 生命周期骨架:
  - `QdrantSidecarConfig`
  - `QdrantSidecarSupervisor`
  - 端口可用性检测。
  - 端口冲突时选择可用端口并报告 degraded 原因。
  - 启动、停止、重启、崩溃标记、状态和健康检查。
  - TCP startup health wait，启动后不可达时标记 degraded。
- 补充 Module 4 文档:
  - `docs/module-4-retrieval.md`
- 增加 `src-tauri/tests/retrieval_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 32 个单元测试、18 个集成测试全部通过。

### 后续

- Module 5 可继续实现 Git 服务、存档点、checkpoint、回档和回档后的索引重建触发。
- 后续 Module 8/9 可基于 Module 4 trait 接入知识库、RAG 和真实 Qdrant/Tantivy 后端。

## 2026-06-23 - 中文代码注释补充

### 已实现

- 按 `模块化实施计划.md` 的注释要求，统一补充中文代码注释。
- 覆盖已实现的核心源码范围:
  - Module 0 core: 错误、端口、工作流、权限、注册表、事件、资源、artifact。
  - Module 1 config: 配置模型、配置存储、迁移、密钥存储。
  - Module 2 costs: 成本模型、SQLite 成本账本、预算、定价估算。
  - Module 3 providers: 请求/响应模型、协议解析、Provider trait、运行时注册表、执行器。
  - Module 4 retrieval: 检索模型、trait、内存后端、混合检索、reranker、Qdrant sidecar 生命周期。
- 将注释重点放在公共函数、公共结构、复杂逻辑块和安全/恢复相关 helper 上。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

## 2026-06-23 - Module 5 Git 服务与版本控制

### 已实现

- 新增 `src-tauri/src/git` Git 服务模块，并在 `src-tauri/src/lib.rs` 导出。
- 定义 Git 数据模型:
  - `GitHealthReport`
  - `GitCommitSummary`
  - `ArchivePoint`
  - `Checkpoint`
  - `RestoreReport`
  - `BranchGraphNode`
- 实现 `GitService`:
  - `init_repository` 幂等初始化仓库。
  - `health_check` 检测非仓库、无 commit 仓库、当前分支、HEAD 和 dirty 状态。
  - `create_archive_point` 创建用户命名存档点 commit。
  - `create_checkpoint` 创建节点级 checkpoint commit。
  - `diff` 返回工作区 diff。
  - `recent_commits` 返回最近 commit 摘要。
  - `branch_graph` 返回简化分支图节点。
  - `restore_to_new_branch` 回档到新分支，不覆盖当前分支。
  - `reinitialize_repository` 保守重新初始化仓库。
- 所有 Git 写操作通过 `Mutex` 串行化，满足 Git 操作互斥要求。
- 回档前强制工作区干净，避免破坏用户未保存改动。
- 回档结果显式标记 `index_rebuild_required` 和 `runtime_rebind_required`。
- 分支名校验拒绝明显危险的 refspec 输入。
- 补充 Module 5 文档:
  - `docs/module-5-git.md`
- 增加 `src-tauri/tests/git_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 34 个单元测试、22 个集成测试全部通过。

### 后续

- Module 6 可继续实现文档服务、artifact 管理和 patch 写回，并接入 Module 5 checkpoint 能力。

## 2026-06-23 - Module 6 文档服务、Artifact 与 Patch 写回

### 已实现

- 新增 `src-tauri/src/documents` 文档服务模块，并在 `src-tauri/src/lib.rs` 导出。
- 定义文档数据模型:
  - `DocumentFormat`
  - `DocumentMetadata`
  - `DocumentReadRequest`
  - `DocumentWriteRequest`
  - `PatchPreview`
  - `PatchApplyReport`
  - `IndexInvalidation`
  - `ArtifactWriteRequest`
- 实现 `DocumentRepository` trait，为后续异步、分片或数据库后端保留替换点。
- 实现 `FileDocumentService`:
  - 支持 Markdown、txt、JSON 打开和保存。
  - JSON 文档写入和读取时进行结构校验。
  - 使用 canonical 文件路径生成 `document_id`。
  - 提供 `document_ref`、`chunk_ref`、`artifact_ref` 生成能力，避免运行状态重复保存大文本。
  - 文件读写在实际打开或写入前复用 Module 0 路径权限沙箱。
  - 保存和 patch 写回后返回 `IndexInvalidation`，供后台增量索引触发。
- 实现 patch 预览与写回:
  - patch hunk 使用 UTF-8 字节范围。
  - 拒绝越界、交叠、倒序和非字符边界范围。
  - 支持 `base_version` 乐观并发校验。
  - 预览返回紧凑 diff 窗口，不携带完整大文本。
  - patch 写回可联动 Module 5 `GitService` 创建 checkpoint。
- 实现 Artifact 写入:
  - Artifact 固定写入配置的 artifact root。
  - 返回 `ArtifactDescriptor`、`storage_uri`、大小和稳定内容哈希。
  - 拒绝 `..`、绝对路径和反斜杠形式的危险 artifact id。
- 补充 Module 6 文档:
  - `docs/module-6-documents.md`
- 增加 `src-tauri/tests/document_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `cargo test --quiet`: 36 个单元测试、28 个集成测试全部通过。

### 后续

- Module 7 可继续实现 LLM 服务与 tool-use，并将模型输出默认转成 patch/artifact 交给 Module 6 写回。
- 后续 UI/后台任务模块需要接入 `IndexInvalidation`，完成真实异步增量索引调度。

## 2026-06-23 - Module 7 LLM 服务与 Tool-use

### 已实现

- 新增 `src-tauri/src/llm` LLM 编排模块，并在 `src-tauri/src/lib.rs` 导出。
- 定义 LLM 服务数据模型:
  - `LlmServiceConfig`
  - `LlmRunRequest`
  - `LlmRunReport`
  - `LlmStreamEvent`
  - `ToolExecutionContext`
  - `ToolExecutionOutput`
  - `LlmAuditEvent`
- 定义 `ToolExecutor` trait，为后续 Module 9 RAG tool 和 Module 10 Skill executor 提供统一入口。
- 实现 `LlmService`:
  - 基础 LLM 生成调用。
  - tool-use 多轮循环。
  - 通过 Module 3 `ProviderExecutor` 调用 provider。
  - provider 返回费用时继续写入 Module 2 成本账本。
  - assistant tool-call 消息和 tool-result 消息会回填到下一轮上下文。
  - 审计日志记录 provider 响应、tool 请求、tool 完成、token、费用和最终控制状态。
- 实现流式事件入口:
  - 调用 provider 时设置 `stream=true`。
  - 当前同步 provider 响应会拆成 `Started`、`Delta`、`ToolCall`、`Finished` 事件。
  - 后续真实 token 级 delta 可在 provider protocol adapter 中接入。
- 实现循环保护:
  - 最大 tool-use 轮次。
  - 总超时。
  - 取消信号。
  - 累计 token 上限。
  - Module 2 预算检查。
  - 普通模式高成本调用转成 `CoreError::Paused`，等待人工确认。
- 为 `TokenUsage` 增加 `Default` 和 `total_tokens()`，方便跨模块统一统计 token。
- 补充 Module 7 文档:
  - `docs/module-7-llm.md`
- 增加 `src-tauri/tests/llm_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `git diff --check`。
- 已执行 `cargo test --quiet`: 38 个单元测试、35 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

### 后续

- Module 8 可继续实现知识库、分层索引与长期记忆，并把结构化抽取/审批结果接入 LLM 服务。
- 后续 provider protocol adapter 需要接入真实流式 delta，使 `LlmStreamEvent` 从契约扩展为实际事件流。

## 2026-06-23 - Module 8 知识库、分层索引与长期记忆

### 已实现

- 新增 `src-tauri/src/knowledge` 知识库模块，并在 `src-tauri/src/lib.rs` 导出。
- 定义分层摘要模型，覆盖 chunk、场景、章节、卷、全书五级摘要。
- 定义结构化事实模型，覆盖角色、别名、关系、世界观、时间线、章节摘要和其他事实类型。
- 所有摘要和事实都强制保留 `source_version` 和 `SourceSpan` 来源引用，避免长期记忆失去可追溯性。
- 定义 `KnowledgeRepository` trait，为后续 SQLite 或混合索引持久化实现保留替换点。
- 实现 `MemoryKnowledgeBase`，用于测试和早期集成。
- 实现 AI 抽取候选事实处理:
  - 普通模式下无审批候选保持 pending，不直接入库。
  - Auto Mode 下无冲突候选可自动入库。
  - 两步审批通过后候选事实入库。
  - 两步审批拒绝后候选不入库。
- 实现冲突队列:
  - 新候选与旧事实冲突时不会静默覆盖旧设定。
  - 冲突记录保留旧事实 id、候选事实、来源、写作理由和判断理由。
  - Auto Mode 下即使两步审批尚未完成，也会先把冲突入队并保留待审信息。
- 实现知识库健康和重建状态:
  - metadata 损坏。
  - 索引损坏。
  - Git 回档后 metadata 与索引同时需要重建。
  - 重建完成后返回处理计数报告。
- 补充 Module 8 文档:
  - `docs/module-8-knowledge.md`
- 增加 `src-tauri/tests/knowledge_contracts.rs` 集成测试。

### 验证情况

- 已执行 `cargo fmt`。
- 已执行 `git diff --check`。
- 已执行 `cargo test --quiet`: 38 个单元测试、42 个集成测试全部通过。
- 已执行 `cargo test --features system-keychain --no-run`: system keychain feature 编译通过。

### 后续

- Module 9 暂等创作总结机制补完后再继续实现 RAG 查询与 SearchProvider。
- 后续持久化阶段需要把当前 `KnowledgeRepository` 接到 SQLite metadata 表，并把可重建索引与事实源分离存储。

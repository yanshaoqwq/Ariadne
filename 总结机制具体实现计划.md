# 总结机制具体实现计划

本文档把 `创作总结机制(不可删除).md` 细化为可执行的工程计划。若本文档、模块化实施计划和架构计划存在冲突，实现时以 `创作总结机制(不可删除).md` 为最高优先级。

## 1. 落地范围

- 下一阶段从 Module 9 开始落地总结机制的基础能力。
- Module 9 先完成工具契约、内置提示词资源、创作知识模型扩展、内存实现和测试。
- 工作流节点编排、真实 agent 调度和审批 UI 放到后续模块继续接入。
- `创作总结机制(不可删除).md` 暂作为需求源保留在工作区，不纳入 git，除非用户明确要求。
- Planner、Detail、Writer、Summarizer 都是独立节点；一个节点就是一个 agent。

## 2. 内置资源

- 新增 `src-tauri/resources/prompt_list.json`。
  - 每一项的 key 是引用名。
  - 每一项必须包含 `prompt` 和 `describe`。
  - 默认提示词使用中文。
  - 代码不得硬编码提示词正文。
- 新增 `src-tauri/resources/display_name.json`。
  - 保存 agent、tool、operation、confirmation item 的显示名称。
  - 代码不得硬编码显示名称。
  - 代码引用 key 时用中文注释标明英文 key 的含义，便于后续本地化。
- Rust 侧新增资源加载和校验逻辑。
  - 使用 `include_str!` 加载内置 JSON。
  - 启动或测试时校验 JSON 合法、必需 key 存在、字段非空。

## 3. 创作知识模型

### 3.1 故事段

- 故事段表示作品中连续且属于同一事件的正文范围。
- 故事段正文不复制保存，只保存 `SourceSpan`。
- 故事段记录包含：
  - `segment_id`
  - 字符串小数编号，例如 `1`、`1.5`、`2`
  - 所属 `chapter_id`
  - 概括文本
  - `SourceSpan`
  - metadata
- 编号排序和中点生成使用十进制字符串算法，不使用 `f64`。
- 在两个故事段之间插入新故事段时，编号取前后编号的中点字符串。

### 3.2 事件

- 事件由一个或多个故事段组成。
- 事件状态支持：
  - `ongoing`
  - `paused`
  - `completed`
- 事件记录包含：
  - `event_id`
  - 概括文本
  - 状态
  - 关联故事段 id 列表
  - 关联章节 id 列表
  - metadata
- 事件与故事段、事件与章节都要建立双向索引。

### 3.3 人物性格与人物关系

- Planner 注册人物初始性格、人物成长、初始关系、关系变化。
- 注册项先进入 `planned` 状态。
- 章节总结阶段找到对应故事段后，注册项转为 `realized`，并建立与故事段的双向索引。
- 人物性格和人物关系变化保留完整版本路径。
- 默认查询返回最近版本。
- find 工具可查询完整变化路径。

### 3.4 伏笔

- 伏笔状态支持：
  - `planned`
  - `planted`
  - `recovered`
  - `abandoned`
- Planner 注册伏笔时创建 `planned`。
- 章节总结识别正文中已埋下伏笔后转为 `planted`。
- 后续章节回收伏笔后转为 `recovered`。
- Planner 放弃未使用伏笔时转为 `abandoned`。
- Planner 上下文默认提供全部未回收伏笔。

### 3.5 双向索引

必须支持以下双向查询：

- 故事段 <-> 章节。
- 事件 <-> 故事段。
- 事件 <-> 章节。
- 人物性格变化 <-> 故事段。
- 人物关系变化 <-> 故事段。
- 伏笔 <-> 故事段。

索引是可重建数据，事实、摘要和注册记录才是源数据。

## 4. 工具契约

### 4.1 Planner 工具

- `planner-register`
  - 用于注册大纲要写到的写作变化。
  - 参数保持机制文档命名：
    - `a`: 功能。
    - `b`: 操作。
    - `c`: 内容。
  - `c` 按功能使用强类型 JSON schema。
  - 首版功能覆盖：
    - 人物性格。
    - 人物关系。
    - 伏笔。
  - 操作支持：
    - `list`
    - `new`
    - `update`
    - `delete`
  - `update` 仅允许人物性格和人物关系变化。
  - `delete` 仅用于删除错误操作结果，不表示角色死亡。
- `planner-find`
  - 查询本地创作知识库。
  - 默认返回编号、摘要、source、score、metadata。
  - 参数可要求附带对应正文文本。
- `planner-search`
  - 用于联网或外部 SearchProvider 考据。
  - 与本地 find 分离。
  - 搜索结果不自动入库。

### 4.2 Detail 工具

- Detail 是同一种节点类型的不同预设。
- 首批预设：
  - 环境。
  - 心理。
  - 设定。
- Detail 工具：
  - `detail-find`
  - `detail-search`
- Detail 可以查询细致文本和外部考据。
- Detail 输出保存到 `details/{chapter_id}.md`，仅用于留档和用户查阅。
- Writer 不通过文件路径读取 Detail 输出；细节内容由系统作为 prompt 直接传给 Writer。

### 4.3 Writer 工具

- Writer 正式写作通过行号 patch 修改正文。
- Writer 写作时允许考据现实情况，但不能注册设定。
- 正文提供给 Writer 时必须带 1-based 行号。
- Writer 工具：
  - `writer-find`
  - `writer-search`
  - `writer-insert-lines`
  - `writer-replace-lines`
- `writer-find` 查询本地创作知识，用于保持正文连续性和设定一致性。
- `writer-search` 在需要知道当前写作位置对应的现实情况时调用外部 SearchProvider。
- `writer-search` 的结果只用于当前写作判断，不自动入库，也不允许转成 register。
- 行号规则：
  - 行号从 1 开始。
  - replace 使用 `start_line` 和 `end_line` 闭区间。
  - insert 使用目标行号位置。
- 系统校验行号 patch 后，将其转换为现有 UTF-8 byte-range `DocumentPatch`。
- 正文写回继续复用现有文档服务和 Git checkpoint。

### 4.4 Summarizer 节点与职责

- 新增 `summarizer` 节点，负责章节总结流水线。
- Summarizer 不是普通工具集合；它是完整节点，内部按固定顺序执行总结步骤。
- Summarizer 负责：
  - 故事段划分并概括。
  - 事件概括。
  - 章节总结。
  - 阶段概括。
  - 检测 Planner 注册项是否在正文中落地。
- 未落地注册项进入问题队列，要求 Planner 给解释。

## 5. Find 范围

find 工具首版必须覆盖：

- 人物当前性格。
- 人物性格完整变化路径。
- 人物当前关系。
- 人物关系完整变化路径。
- 某事件对应的故事段。
- 某故事段对应的正文文本。
- 未回收伏笔。
- 指定伏笔的埋下和回收位置。
- 章节总结。
- 阶段总结。

默认返回轻量结果；只有调用参数明确要求正文时才返回对应文本。

## 6. Agent 上下文

### 6.1 Planner 节点 / PlannerAgent

Planner 节点就是 PlannerAgent，用于生成新章节大纲。

Planner prompt 组装包含：

- 前文总结内容。
  - 当前阶段提供全部章节总结。
  - 非当前阶段提供阶段总结。
- 人物及人物当前性格。
- 全部未回收伏笔。
- 上一章全文。

Planner 输出保存到 `outlines/{chapter_id}.md`，用于留档和用户查阅。

### 6.2 Detail 节点 / DetailAgent

Detail 节点就是 DetailAgent，用于生成当前章节的细节材料。

- 可以有环境、心理、设定等预设。
- 可以使用 `detail-find` 和 `detail-search`。
- 输出保存到 `details/{chapter_id}.md`。
- 输出内容由系统直接塞进 Writer prompt，不要求 Writer 读文件。

### 6.3 Writer 节点 / WriterAgent

Writer 节点就是 WriterAgent，用于正式写作。

Writer prompt 组装包含：

- 写作提示词。
- 上一章内容。
- 本章大纲。
- 本章细节。
- 带行号的当前章节正文或待写入位置上下文。

Writer 可以使用 `writer-find` 和 `writer-search` 做本地查询与现实考据。
Writer 默认不接收可回收伏笔类大纲信息。
Writer 不具备 register 工具，不能注册人物性格、人物关系或伏笔。

## 7. 章节总结流水线

触发时机：

- 正式写作通过审核后触发。

执行顺序：

1. 故事段划分并概括。
   - 为故事段编号。
   - 默认使用上一个编号的下一个整数。
   - 手动插入故事段时使用字符串中点编号。
   - 建立故事段与章节的双向索引。
2. 事件概括。
   - 将多个故事段归入同一事件。
   - 或将故事段加入已有事件。
   - 有变动的事件重新生成概括和状态。
   - 建立事件与故事段、事件与章节的双向索引。
3. 章节总结。
   - 总结当前章节写了什么。
   - 写出各事件进展。
   - 输入包含章节内容、事件变动对比、人物性格和关系变动对比。
4. 阶段概括。
   - 判断本章是否仍属于当前阶段。
   - 若不是，LLM 提案创建新阶段。
   - 普通模式需要确认或按配置跳过。
   - Auto Mode 默认自动审计。
   - 无论是否新阶段，都更新包含本章的阶段总结。

## 8. 确认项和 Auto Mode

确认项包括：

- planner-register 的所有子功能。
- 故事段划分并概括。
- 事件概括。
- 章节总结。
- 阶段概括。
- Writer 自动修正文档 patch。

普通模式：

- 每类确认项都可以独立配置为人工确认或跳过。
- 默认需要人工确认。

Auto Mode：

- 默认对确认项执行自动审计。
- 每类确认项可配置为自动跳过或自动审计。
- 自动审计 prompt 从 `prompt_list.json` 读取。
- 成本控制和硬权限仍然可以阻断运行。

确认或审计失败：

- 暂停当前流水线。
- 记录待处理项。
- 等待修正或人工处理。

## 9. Planner 与 Summarizer 通信

- 未落地注册项会生成问题队列项。
- 问题队列项包含：
  - 注册项 id。
  - 预期变化。
  - Summarizer 未匹配到的原因。
  - 相关章节、故事段、正文引用。
- Planner 需要针对问题给出解释。
- 若需要自动修正文档：
  - Planner 提供解释和修正目标。
  - Writer 根据解释和正文上下文生成行号 patch。
  - patch 进入确认项。
  - Auto Mode 默认自动审计。
  - 通过后写回正文。
  - 写回后从受影响步骤重新跑章节总结流水线。

## 10. Module 9 实施切分

### 10.1 第一批提交：资源与模型

- 新增内置资源目录和两个 JSON 文件。
- 新增资源加载和校验代码。
- 扩展知识库模型：
  - 故事段。
  - 事件。
  - 注册项。
  - 伏笔。
  - 问题队列项。
  - 双向索引。
- 增加字符串小数编号工具。
- 增加模型单元测试。

### 10.2 第二批提交：工具 schema 与执行器

- 新增 Module 9 RAG/创作工具模块。
- 定义 planner/detail/writer/summarizer 工具 schema。
- 实现 `planner-register` 内存执行。
- 实现 `planner-find` / `detail-find` / `writer-find` 本地查询。
- 实现 `planner-search` / `detail-search` / `writer-search` SearchProvider adapter。
- 保证 search 不自动写知识库。

### 10.3 第三批提交：章节总结流水线契约

- 定义章节总结流水线请求和报告。
- 定义确认项状态和 Auto Mode 审计策略。
- 定义 Planner/Summarizer 问题队列。
- 定义 Writer 行号 patch 到 `DocumentPatch` 的转换。
- 增加契约测试。

### 10.4 第四批提交：文档与计划同步

- 更新 `项目完成情况.md`。
- 更新 `实施记录.md`。
- 更新 `模块化实施计划.md` 的 Module 9 状态。
- 新增 `docs/module-9-rag-writing-tools.md`。
- 本地 commit 并 push。

## 11. 测试计划

- 资源测试：
  - `prompt_list.json` 可加载且每项包含非空 `prompt` 和 `describe`。
  - `display_name.json` 可加载且必需 key 存在。
  - 工具描述和显示名不从代码硬编码。
- 编号测试：
  - `1 < 1.5 < 2` 排序稳定。
  - 多次插入中点不出现浮点误差。
- 模型测试：
  - 故事段只保存 `SourceSpan`，不复制正文。
  - 事件与故事段、章节可双向查询。
  - 人物性格和关系变化保留完整路径。
  - 伏笔状态转换正确。
- 工具测试：
  - Planner 仅暴露 `planner-register`、`planner-find`、`planner-search`。
  - Detail 暴露 `detail-find`、`detail-search`。
  - Writer 暴露 `writer-find`、`writer-search`、`writer-insert-lines`、`writer-replace-lines`。
  - Writer 不暴露 `planner-register`。
  - `planner-register` 拒绝非法 operation 和非法强类型 `c`。
  - find 默认返回轻量结果，显式要求时返回正文文本。
  - search 不自动写入知识库。
- 流水线测试：
  - 审核通过后按顺序执行故事段、事件、章节、阶段总结。
  - 确认失败时暂停。
  - 未落地注册项生成问题队列。
  - Writer 修正 patch 写回后重跑受影响总结步骤。
- 验证命令：
  - `cargo fmt`
  - `git diff --check`
  - `cargo test --quiet`
  - `cargo test --features system-keychain --no-run`

## 12. 提交要求

- 每个阶段完成后更新 `项目完成情况.md`，说明实现了什么。
- 代码必须保留中文注释。
- 不提交 `项目检验报告/`，除非用户明确要求。
- 不提交 `创作总结机制(不可删除).md`，除非用户明确要求。
- Module 9 完成后本地 commit 并 push 到 `origin/main`。

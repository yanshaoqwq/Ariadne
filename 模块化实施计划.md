# Literature Agent - 模块化实施计划

**制定日期**: 2026-06-22  
**对应架构**: 《项目总计划-架构版(禁止删除).md》v4.0  
**实施原则**: Module 0 先定义核心契约，后续模块以新增实现和注册接入为主，避免反复大改旧模块。  
**MVP 目标**: 完整能力 MVP，至少百万字真实项目可用。

---

## 核心架构：契约先行的分层模块设计

```
┌─────────────────────────────────────────────────────────────┐
│ Module 12: 前端工作区、快捷命令、导出                         │
│ Module 11: 工作流引擎、受限循环、审批模块                     │
│ Module 10: Skill 系统（LLM / HTTP / WASM）                   │
├─────────────────────────────────────────────────────────────┤
│ Module 9: RAG 查询与 SearchProvider                         │
│ Module 7: LLM 服务与 tool-use                               │
│ Module 6: 文档服务、artifact、patch 写回                     │
├─────────────────────────────────────────────────────────────┤
│ Module 8: 知识库、分层索引、长期记忆、冲突队列                 │
│ Module 5: Git 服务、存档点、checkpoint、回档                  │
│ Module 4: VectorStore / FullTextStore / Reranker             │
├─────────────────────────────────────────────────────────────┤
│ Module 3: Provider 抽象（OpenAI / Anthropic / Gemini）        │
│ Module 2: 成本追踪、预算、预授权                              │
│ Module 1: 配置、密钥、迁移                                    │
├─────────────────────────────────────────────────────────────┤
│ Module 0: 核心契约、typed ports、注册表、事件、错误、资源池     │
└─────────────────────────────────────────────────────────────┘
```

---

## Module 0: 核心契约与公共基础

**目标**: 先固定跨模块契约，使后续模块只新增实现并接入公共接口。

### 0.1 模块职责

- 定义 Node、Edge、Workflow、Port、Artifact、Provider、Skill 的公共数据结构。
- 定义 typed input/output ports，支持多输入多输出。
- 定义大文本引用传递，禁止百万字内容在节点间默认内联复制。
- 定义模块注册表: NodeRegistry、SkillRegistry、ProviderRegistry。
- 定义统一错误模型、事件模型、运行状态、资源池和取消信号。
- 定义 Auto Mode、审批、权限硬限制的公共语义。

### 0.2 文件结构

```
src-tauri/src/core/
├── mod.rs
├── ports.rs
├── workflow.rs
├── artifacts.rs
├── registry.rs
├── events.rs
├── errors.rs
├── resources.rs
└── permissions.rs
```

### 0.3 核心契约

```rust
pub enum PortValue {
    Inline(serde_json::Value),
    DocumentRef { document_id: String, range: Option<TextRange> },
    ChunkRef { chunk_id: String },
    ArtifactRef { artifact_id: String },
}

pub struct PortDefinition {
    pub name: String,
    pub type_name: String,
    pub required: bool,
}

pub struct NodeDefinition {
    pub type_name: String,
    pub input_ports: Vec<PortDefinition>,
    pub output_ports: Vec<PortDefinition>,
    pub supports_checkpoint: bool,
    pub supports_auto_approval: bool,
}

pub enum RunControl {
    Continue,
    Pause,
    Stop,
}

pub struct LoopPolicy {
    pub max_iterations: u32,
    pub timeout_ms: u64,
    pub budget_limit_usd: Option<f64>,
    pub stop_condition: serde_json::Value,
}
```

### 0.4 Auto Mode 语义

- Auto Mode 不修改权限配置项。
- 执行时先判断 Auto Mode；开启时跳过普通人工确认。
- 预算、路径沙箱、WASM 沙箱、密钥保护始终是硬限制。
- 审批行为由节点级配置决定；全局只提供可用审批提示词配置。

### 0.5 验收标准

- [ ] 所有模块只依赖 Module 0 类型和接口通信。
- [ ] typed ports 支持审批意见、正文、patch、artifact、成本等独立输出。
- [ ] 大文本通过引用传递。
- [ ] Pause 与 Stop 语义在核心状态模型中明确区分。

---

## Module 1: 配置管理、密钥存储与迁移

**目标**: 管理项目配置、全局密钥、配置 schema 和迁移。

### 1.1 模块职责

- 读取和写入分文件 YAML 配置。
- 使用系统 keychain 存储全局密钥，项目仅引用 key id。
- 配置文件带 schema version，启动时自动迁移。
- 支持 provider、permissions、rag、workflow、git、auto mode 等配置。

### 1.2 配置布局

```
.config/
├── app.yaml
├── providers.yaml
├── permissions.yaml
├── rag.yaml
├── workflow.yaml
├── git.yaml
└── auto_mode.yaml
```

### 1.3 关键配置

- `providers.yaml`: OpenAI、Anthropic、Gemini、OpenAI-compatible base_url、Embedding、Reranker、SearchProvider。
- `permissions.yaml`: 网络、Web 搜索、HTTP Skill、WASM 能力、文件路径白名单。
- `auto_mode.yaml`: Auto Mode 默认值、预算预授权、可用审批提示词。
- `git.yaml`: 文档、工作流、Skill、配置是否入库；缓存和数据库默认忽略。

### 1.4 验收标准

- [ ] 密钥不落项目文件。
- [ ] 配置和数据库迁移均可重复执行。
- [ ] Auto Mode 配置不会改写权限开关。
- [ ] OpenAI-compatible 本地服务可通过 base_url 配置。

---

## Module 2: 成本追踪与预算控制

**目标**: 所有模型和外部 API 调用可追踪、可预估、可限制。

### 2.1 模块职责

- 记录 LLM、Embedding、Reranker、Search API、HTTP Skill、冲突审批调用。
- 支持单次、每日、月度预算。
- 普通模式执行前确认高成本操作。
- Auto Mode 在预算预授权内自动执行，超限触发 Pause。

### 2.2 数据存储

- `costs.db`: API 调用、模型、tokens、费用、workflow_id、node_id、tool_call_id。
- 预算配置来自 `.config/auto_mode.yaml` 和 `.config/providers.yaml`。

### 2.3 验收标准

- [ ] 所有 provider 调用都经过成本记录。
- [ ] tool-use 多轮调用按实际发生费用入账。
- [ ] 超预算时工作流 Pause，而不是静默失败。

---

## Module 3: Provider 抽象与模型接入

**目标**: 用统一接口接入 LLM、Embedding、Reranker 和 SearchProvider。

### 3.1 模块职责

- 定义 Provider trait 和请求/响应标准结构。
- 支持 OpenAI、Anthropic、Gemini 三类协议格式。
- OpenAI-compatible `base_url` 支持本地模型服务。
- Provider 调用统一接入成本追踪、日志、重试和超时。

### 3.2 Provider 类型

- LLMProvider
- EmbeddingProvider
- RerankerProvider
- SearchProvider

### 3.3 验收标准

- [ ] LLM/Embedding/Reranker 可以独立切换 provider。
- [ ] Gemini 与 Anthropic tool-use 差异被封装在 provider 层。
- [ ] 本地 OpenAI-compatible 服务不需要单独代码路径。

---

## Module 4: 向量、全文与重排检索

**目标**: 提供高性能混合检索基础。

### 4.1 模块职责

- VectorStore 抽象，默认应用托管本地 Qdrant sidecar。
- FullTextStore 抽象，默认 Tantivy。
- Reranker 抽象，默认启用，可选本地或远程 provider。
- 支持后台批量写入、增量删除、冷热层索引。

### 4.2 实施要求

- 不假设 Rust SDK 直接嵌入 Qdrant。
- sidecar 生命周期由应用管理: 启动、健康检查、停止、端口冲突处理。
- 检索返回必须包含来源引用和 score。

### 4.3 验收标准

- [ ] 百万字项目混合检索可用。
- [ ] 主 UI 不因索引或检索阻塞。
- [ ] VectorStore 可替换，不影响 RAG 和知识库模块。

---

## Module 5: Git 服务与版本控制

**目标**: 提供可追溯、可恢复、可配置的 Git 集成。

### 5.1 模块职责

- 初始化和健康检查 Git 仓库。
- 创建用户命名存档点。
- 创建节点级 checkpoint commit。
- 支持 diff、分支图、回档到新分支。
- 回档后触发索引重建。

### 5.2 跟踪策略

- 文档、工作流、Skill、非敏感配置是否入库由配置决定。
- 缓存、向量库、全文索引、成本库、运行状态库默认忽略。
- Stop 不回滚已有 Git 历史。

### 5.3 验收标准

- [ ] Git 操作全局互斥。
- [ ] 回档不会破坏当前工作，必须创建新分支。
- [ ] Git 损坏时支持检测、备份、降级和重新初始化，不承诺自动修复所有损坏。

---

## Module 6: 文档服务、Artifact 与 Patch 写回

**目标**: 高性能管理 Markdown、txt、JSON 和生成结果写回。

### 6.1 模块职责

- 支持 Markdown、txt、JSON。
- 管理 document_ref、chunk_ref、artifact_ref。
- LLM/Skill 输出默认生成 patch/diff。
- 普通模式预览确认，Auto Mode 按节点配置自动应用。
- 保存后触发后台增量索引。

### 6.2 验收标准

- [ ] 百万字项目中文档打开、保存、patch 应用不阻塞 UI。
- [ ] 大文本不在运行状态中重复复制。
- [ ] patch 写回和 Git checkpoint 可联动。

---

## Module 7: LLM 服务与 Tool-use

**目标**: 统一模型调用、流式输出、工具调用和运行日志。

### 7.1 模块职责

- 调用 Module 3 provider。
- 支持流式输出。
- 支持 tool-use 多轮循环保护。
- 将工具调用、模型输出、tokens、费用写入运行日志和成本账本。

### 7.2 循环保护

- 最大 tool-use 轮次。
- 超时。
- token 和预算检查。
- 用户 Pause/Stop 信号。

### 7.3 验收标准

- [ ] OpenAI、Anthropic、Gemini 协议都可完成基础生成。
- [ ] tool-use 过程可审计。
- [ ] Pause/Stop 能中断长调用后的后续流程。

---

## Module 8: 知识库、分层索引与长期记忆

**目标**: 支撑至少百万字小说的长期上下文。

### 8.1 模块职责

- 文档 chunk、场景、章节、卷、全书分层摘要。
- 角色、别名、关系、世界观、时间线、章节摘要结构化数据。
- AI 抽取候选数据，普通模式人工确认。
- Auto Mode 自动通过普通确认，但冲突不覆盖。
- 冲突队列和两步 LLM 审批。

### 8.2 冲突审批

1. 写作 LLM 输出“为什么这样写合理”的理由。
2. 独立判断提示词的 LLM 审批。
3. 审批结果、理由、来源进入待审队列。

### 8.3 验收标准

- [ ] 每条摘要和事实都保留来源和版本。
- [ ] 冲突不会静默覆盖旧设定。
- [ ] Auto Mode 不阻塞，但待审信息完整。

---

## Module 9: RAG 查询与 SearchProvider

**目标**: 为 LLM 和 SearchNode 提供少工具强参数的检索能力。

### 9.1 模块职责

- 统一 RAG 工具 schema。
- 使用 `operation` 参数承载语义搜索、结构化查询、邻近上下文、摘要查询。
- 混合召回: Qdrant + Tantivy + SQLite。
- reranker 重排后返回引用。
- 本地知识库搜索和 Web SearchProvider 分离。

### 9.2 验收标准

- [ ] LLM 可通过同一 RAG 工具执行不同子操作。
- [ ] SearchNode 可搜索本地知识库和可配置 Web provider。
- [ ] 检索结果包含引用、score、来源和片段。

---

## Module 10: Skill 系统

**目标**: 支持可复用、可参数化、可沙箱执行的 Skill。

### 10.1 模块职责

- 加载全局 Skill 和项目 Skill，项目优先。
- Skill schema 生成 typed ports。
- 执行 LLM Executor、HTTP Executor、WASM Script Executor。
- HTTP 和 WASM 执行受权限、预算、超时、日志约束。

### 10.2 WASM 安全

- 文件访问必须走授权路径。
- 网络访问默认禁止，除非权限允许。
- 限制运行时间、内存、输出大小。
- 不支持不受控本机脚本。

### 10.3 验收标准

- [ ] 三类 executor 都能在工作流中执行。
- [ ] WASM 越权访问被拒绝。
- [ ] Skill 输出可连接到下游 typed ports。

---

## Module 11: 工作流引擎与审批模块

**目标**: 执行完整 Agent 工作流，支持受限循环、审批、恢复和可观测性。

### 11.1 模块职责

- 执行 typed port 工作流。
- 支持 LLM、Document、Skill、Search、Condition/Eval、Loop/Trigger、Approval、Export 节点。
- 支持受限循环。
- 支持 Pause、Stop、Resume。
- 保存运行状态、节点输出引用、工具调用、审批意见、成本和错误。

### 11.2 Pause / Stop

- Pause: 保存运行态，可继续。
- Stop: 终止当前运行，保留已完成节点、artifact、checkpoint 和 Git 历史。

### 11.3 审批模块

- 节点级配置是否 Auto Mode 自动审批。
- 节点级选择审批提示词。
- 审批 LLM 意见作为输出引脚。

### 11.4 验收标准

- [ ] 受限循环必须有最大轮次、预算、超时和终止条件。
- [ ] 失败后能从最近安全点恢复。
- [ ] Stop 后不能丢失已完成结果。
- [ ] 审批输出可被下游节点使用。

---

## Module 12: 前端工作区、快捷命令与导出

**目标**: 提供编辑器和节点画布并重的桌面体验。

### 12.1 模块职责

- Tauri 2 + React + TypeScript。
- CodeMirror 6 编辑 Markdown、txt、JSON。
- React Flow 节点画布，显示 typed ports。
- 运行面板显示日志、工具调用、成本、Pause/Stop/Resume。
- Git 面板显示存档点、checkpoint、diff、分支图。
- RAG/知识库面板显示索引、冲突、待审队列。
- 支持基础 EPUB/PDF 导出。

### 12.2 项目管理

- 一次打开一个项目。
- 支持最近项目和项目切换。
- 多项目并行由用户开启多个窗口，不在单窗口内调度。

### 12.3 验收标准

- [ ] 编辑器和节点画布都可作为主工作区。
- [ ] 百万字项目操作不阻塞主 UI。
- [ ] 用户可以查看完整运行日志和成本。
- [ ] 可导出基础 EPUB/PDF。

---

## 实施顺序

1. **Module 0**: 核心契约与公共基础。
2. **Module 1-3**: 配置、成本、Provider。
3. **Module 4-6**: 检索后端、Git、文档与 artifact。
4. **Module 7-9**: LLM、知识库、RAG/Search。
5. **Module 10-11**: Skill、工作流、审批。
6. **Module 12**: 前端整合、快捷命令、导出。

每个模块完成时必须补齐:
- 模块职责
- 公共接口
- 数据模型
- 错误和权限边界
- 单元测试
- 集成测试
- 前端或 IPC 接入点

---

## MVP 集成验收场景

1. **百万字项目人工验收**
   - 导入真实百万字项目。
   - 完成索引、检索、写作、patch 写回、Git checkpoint、回档和恢复。

2. **混合 RAG**
   - 同一查询触发向量、全文、结构化召回和 reranker。
   - 返回结果包含来源引用。

3. **受限循环**
   - LLM 生成内容，Eval 判断不通过，Loop 进入下一轮。
   - 达到条件或最大轮次后退出。

4. **Pause / Stop**
   - Pause 后可继续。
   - Stop 后运行终止，但已完成节点、Git、artifact、checkpoint 保留。

5. **审批模块**
   - 普通模式人工审批。
   - Auto Mode 节点级自动审批。
   - 审批 LLM 意见通过输出引脚传给下游。

6. **知识冲突**
   - 新内容与旧设定冲突。
   - 写作 LLM 给出合理性理由。
   - 独立审批 LLM 给出判断。
   - 冲突进入待审队列。

7. **Skill 执行**
   - LLM、HTTP、WASM 三类 Skill 成功执行。
   - WASM 越权访问被拒绝。

8. **成本与 Auto Mode**
   - Auto Mode 在预算内自动执行。
   - 超预算时工作流 Pause。

9. **Git 策略**
   - 文档/工作流/Skill 入库策略可配置。
   - 缓存、索引、成本库默认忽略。

10. **导出**
    - Markdown 项目导出基础 EPUB/PDF。

---

## 待办事项

- [ ] Module 0: 核心契约与公共基础
- [ ] Module 1: 配置管理、密钥存储与迁移
- [ ] Module 2: 成本追踪与预算控制
- [ ] Module 3: Provider 抽象与模型接入
- [ ] Module 4: 向量、全文与重排检索
- [ ] Module 5: Git 服务与版本控制
- [ ] Module 6: 文档服务、Artifact 与 Patch 写回
- [ ] Module 7: LLM 服务与 Tool-use
- [ ] Module 8: 知识库、分层索引与长期记忆
- [ ] Module 9: RAG 查询与 SearchProvider
- [ ] Module 10: Skill 系统
- [ ] Module 11: 工作流引擎与审批模块
- [ ] Module 12: 前端工作区、快捷命令与导出

---

## 估算说明

原“每个模块 2-3 天，总计 6-8 周”的估算不适用于 v4.0 完整 MVP。当前范围包含百万字性能、受限循环、完整 Skill、审批、Auto Mode、混合 RAG、WASM 沙箱和导出。排期必须在 Module 0 完成后按技术验证结果重新估算。
